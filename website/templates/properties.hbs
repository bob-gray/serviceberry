{{#each properties}}
  - **{{name}}** *{{type}}* {{#unless required}}[optional]{{/unless}}{{#if description}}

    {{description}} {{#if hasDefault}}**Default:** {{default}}{{/if}}{{/if}}
  {{> properties}}

{{/each}}